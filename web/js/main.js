// Short named redirect function so that it is easy to type
function rd(href) {
    window.location.href = href;
}

// short script for toggling "dark mode"
function darkMode() {
    var body = document.body;
    body.classList.toggle("dark-mode");
}